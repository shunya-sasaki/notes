# Zellij

## Install

Option 1. Homebrew

```sh
brew install zellij
```

## Config

### Initialize config file

`~/.config/zellij` ディレクトリを作成し、以下のコマンドを実行して設定ファイルを作成します。

```sh
zellij setup --dump-config > ~/.config/zellij/config.kdl
```
