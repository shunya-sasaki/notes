# Zellij

## Install

Option 1. Homebrew

```sh
brew install zellij
```

## Config

### Initialize config file


Create `~/.config/zellij` directory, and run the following command to create a config file.

```sh
zellij setup --dump-config > ~/.config/zellij/config.kdl
```
